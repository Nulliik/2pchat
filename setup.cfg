[flake8]
max-line-length = 100
extend-ignore = E203
exclude = .git,__pycache__,.venv,.mypy_cache
per-file-ignores = __init__.py:F401

[tool:pytest]
addopts = -ra
testpaths = messenger/tests
markers =
    live_network: requires external network services and is disabled by default
