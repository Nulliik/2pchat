# Repository Agent Instructions

- Before delivering work, install dependencies from `messenger/requirements.txt` so tests can run in this environment.
- Run the full test suite (`pytest` unless otherwise specified) before finalizing changes and report the results.
